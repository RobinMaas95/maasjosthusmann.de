"use client";
import {
  useIsomorphicLayoutEffect
} from "./chunk-O2DNTQMZ.js";
import {
  registerIcons
} from "./chunk-WP43CGFQ.js";
import {
  ThemeContext,
  dataTokenDefaults,
  defineTheme,
  domainTokenDefaults,
  expandColorScale,
  expandMotionScale,
  expandRadiusScale,
  expandTypeScale,
  generateOnMediaCSS,
  generateThemeCSS,
  generateThemeCSSFlat,
  generateThemeRules,
  generateThemeRulesSplit,
  generateTypeScaleComponents,
  isDefinedTheme,
  resolveThemeToken,
  resolveThemeTokens,
  syntaxTokenDefaults,
  tokenDefaults,
  tokenVar,
  tokenVars,
  useMediaQuery,
  useTheme
} from "./chunk-XPWH7K3D.js";
import {
  devWarn,
  warnOnce
} from "./chunk-R5HKDH7J.js";
import {
  borderDefaults,
  borderVars,
  colorDefaults,
  colorVars,
  dataAttr,
  durationDefaults,
  durationVars,
  easeDefaults,
  easeVars,
  fontWeightDefaults,
  fontWeightVars,
  props,
  radiusDefaults,
  radiusVars,
  shadowDefaults,
  shadowVars,
  sizeDefaults,
  sizeVars,
  spacingDefaults,
  spacingVars,
  textSizeDefaults,
  textSizeVars,
  transitionDefaults,
  transitionVars,
  typeScaleDefaults,
  typeScaleVars,
  typographyDefaults,
  typographyVars
} from "./chunk-WEKT4OCY.js";
import {
  require_jsx_runtime
} from "./chunk-YCAIEUY5.js";
import {
  __toESM,
  require_react
} from "./chunk-MB6QVOVQ.js";

// node_modules/@astryxdesign/core/dist/theme/Theme.js
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var wrapperStyles = {
  base: {
    k1xSpc: "xjp7ctv",
    kMwMTN: "x1tgivj0",
    kMv6JI: "x9ynric",
    $$css: true
  },
  light: {
    kQNsl9: "x19aimcq",
    $$css: true
  },
  dark: {
    kQNsl9: "xntwwlm",
    $$css: true
  },
  system: {
    kQNsl9: "x108lcm5",
    $$css: true
  }
};
var ThemeNestingContext = import_react.default.createContext(false);
ThemeNestingContext.displayName = "ThemeNestingContext";
var injectedThemes = /* @__PURE__ */ new Set();
function useThemeStyleInjection(theme) {
  const id = (0, import_react.useId)();
  (0, import_react.useInsertionEffect)(() => {
    if (theme.__built) {
      return;
    }
    const themeKey = `astryx-theme-${theme.name}`;
    if (injectedThemes.has(themeKey)) {
      return;
    }
    warnOnce(`theme-injection:${theme.name}`, "Theme", `"${theme.name}" is using runtime style injection. For better performance, use the pre-built theme:

  import {${theme.name}Theme} from '@astryxdesign/theme-${theme.name}/built';
  import '@astryxdesign/theme-${theme.name}/theme.css';

For custom themes, run \`npx @astryxdesign/cli theme build <file>\` to generate the built artifacts.`);
    const {
      prose,
      component
    } = generateThemeCSS(theme);
    if (!prose && !component) {
      return;
    }
    if (prose) {
      const proseStyle = document.createElement("style");
      proseStyle.setAttribute(dataAttr("theme-prose"), theme.name);
      proseStyle.setAttribute(dataAttr("id"), id);
      proseStyle.textContent = `@layer reset {
${prose}
}`;
      document.head.appendChild(proseStyle);
    }
    if (component) {
      const compStyle = document.createElement("style");
      compStyle.setAttribute(dataAttr("theme"), theme.name);
      compStyle.setAttribute(dataAttr("id"), id);
      compStyle.textContent = `@layer astryx-theme {
${component}
}`;
      document.head.appendChild(compStyle);
    }
    injectedThemes.add(themeKey);
    return () => {
      const proseEl = document.querySelector(`style[${dataAttr("theme-prose")}="${theme.name}"][${dataAttr("id")}="${id}"]`);
      const compEl = document.querySelector(`style[${dataAttr("theme")}="${theme.name}"][${dataAttr("id")}="${id}"]`);
      proseEl?.remove();
      compEl?.remove();
      injectedThemes.delete(themeKey);
    };
  }, [theme, id]);
}
function useRootThemeSync(isNested, mode, themeName) {
  useIsomorphicLayoutEffect(() => {
    if (isNested) {
      return;
    }
    if (typeof document === "undefined") {
      return;
    }
    if (mode === "light" || mode === "dark") {
      document.documentElement.setAttribute("data-theme", mode);
    } else {
      document.documentElement.removeAttribute("data-theme");
    }
    document.documentElement.setAttribute(dataAttr("theme"), themeName);
    return () => {
      document.documentElement.removeAttribute("data-theme");
      document.documentElement.removeAttribute(dataAttr("theme"));
    };
  }, [isNested, mode, themeName]);
}
function Theme({
  theme,
  mode = "system",
  children
}) {
  const isNested = (0, import_react.use)(ThemeNestingContext);
  useThemeStyleInjection(theme);
  useRootThemeSync(isNested, mode, theme.name);
  const colorSchemeStyle = mode === "dark" ? wrapperStyles.dark : mode === "light" ? wrapperStyles.light : wrapperStyles.system;
  const icons = theme.icons;
  if (icons != null) {
    registerIcons(icons);
  }
  const ctxValue = (0, import_react.useMemo)(() => ({
    theme,
    mode
  }), [theme, mode]);
  return (0, import_jsx_runtime.jsx)(ThemeContext, {
    value: ctxValue,
    children: (0, import_jsx_runtime.jsx)(ThemeNestingContext, {
      value: true,
      children: (0, import_jsx_runtime.jsx)("div", {
        ...props(wrapperStyles.base, colorSchemeStyle),
        "data-astryx-theme": theme.name,
        "data-theme": mode === "system" ? void 0 : mode,
        children
      })
    })
  });
}
Theme.displayName = "Theme";

// node_modules/@astryxdesign/core/dist/theme/MediaTheme.js
var React2 = __toESM(require_react(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
function MediaTheme({
  mode,
  children
}) {
  return (0, import_jsx_runtime2.jsx)("div", {
    [dataAttr("media")]: mode,
    ...{
      className: "xjp7ctv x1tgivj0"
    },
    children
  });
}
MediaTheme.displayName = "MediaTheme";

// node_modules/@astryxdesign/core/dist/theme/syntax/defineSyntaxTheme.js
var CSS_PREFIX = "--color-syntax-";
function toCSSProperty(key) {
  return CSS_PREFIX + key;
}
var ALL_SYNTAX_KEYS = Object.keys(syntaxTokenDefaults).map((k) => k.replace(CSS_PREFIX, ""));
function resolveTokenValue(value) {
  if (Array.isArray(value)) {
    return `light-dark(${value[0]}, ${value[1]})`;
  }
  return value;
}
function resolveSyntaxTokenForMode(value, mode) {
  if (Array.isArray(value)) {
    return mode === "dark" ? value[1] : value[0];
  }
  const match = value.match(/^light-dark\(([^,]+),([^)]+)\)$/);
  if (match) {
    return mode === "dark" ? match[2].trim() : match[1].trim();
  }
  return value;
}
function defineSyntaxTheme(input) {
  const missing = ALL_SYNTAX_KEYS.filter((key) => !(key in input.tokens));
  if (missing.length > 0) {
    devWarn("defineSyntaxTheme", `"${input.name}": missing tokens: ${missing.join(", ")}. All 14 syntax tokens are required.`);
  }
  const resolved = {};
  for (const key of ALL_SYNTAX_KEYS) {
    resolved[key] = resolveTokenValue(input.tokens[key]);
  }
  return {
    name: input.name,
    tokens: resolved,
    __inputTokens: {
      ...input.tokens
    }
  };
}
function syntaxThemeStyle(theme) {
  const vars = {};
  for (const key of ALL_SYNTAX_KEYS) {
    vars[toCSSProperty(key)] = theme.tokens[key];
  }
  return vars;
}

// node_modules/@astryxdesign/core/dist/theme/syntax/SyntaxTheme.js
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var SyntaxThemeContext = (0, import_react2.createContext)(null);
SyntaxThemeContext.displayName = "SyntaxThemeContext";
function useSyntaxTheme() {
  const ctx = (0, import_react2.use)(SyntaxThemeContext);
  const prefersDark = useMediaQuery("(prefers-color-scheme: dark)");
  const effectiveMode = prefersDark ? "dark" : "light";
  const tokens = (0, import_react2.useMemo)(() => {
    if (!ctx) {
      return null;
    }
    const resolved = {};
    for (const key of ALL_SYNTAX_KEYS) {
      resolved[key] = resolveSyntaxTokenForMode(ctx.theme.__inputTokens[key], effectiveMode);
    }
    return resolved;
  }, [ctx, effectiveMode]);
  if (!ctx || !tokens) {
    return null;
  }
  return {
    name: ctx.theme.name,
    mode: effectiveMode,
    token: (name) => tokens[name] ?? "",
    tokens
  };
}
function SyntaxTheme({
  theme,
  children
}) {
  const style = (0, import_react2.useMemo)(() => syntaxThemeStyle(theme), [theme]);
  const value = (0, import_react2.useMemo)(() => ({
    theme
  }), [theme]);
  return (0, import_jsx_runtime3.jsx)(SyntaxThemeContext, {
    value,
    children: (0, import_jsx_runtime3.jsx)("div", {
      style,
      "data-astryx-syntax-theme": theme.name,
      children
    })
  });
}
SyntaxTheme.displayName = "SyntaxTheme";

// node_modules/@astryxdesign/core/dist/theme/syntax/presets.js
var oneDarkPro = defineSyntaxTheme({
  name: "one-dark-pro",
  tokens: {
    keyword: "#c678dd",
    string: "#98c379",
    comment: "#5c6370",
    number: "#d19a66",
    function: "#61afef",
    type: "#e5c07b",
    variable: "#e06c75",
    operator: "#56b6c2",
    constant: "#d19a66",
    tag: "#e06c75",
    attribute: "#d19a66",
    property: "#e06c75",
    punctuation: "#abb2bf",
    background: "#282c34"
  }
});
var dracula = defineSyntaxTheme({
  name: "dracula",
  tokens: {
    keyword: "#ff79c6",
    string: "#f1fa8c",
    comment: "#6272a4",
    number: "#bd93f9",
    function: "#50fa7b",
    type: "#8be9fd",
    variable: "#f8f8f2",
    operator: "#ff79c6",
    constant: "#bd93f9",
    tag: "#ff79c6",
    attribute: "#50fa7b",
    property: "#66d9ef",
    punctuation: "#f8f8f2",
    background: "#282a36"
  }
});
var monokai = defineSyntaxTheme({
  name: "monokai",
  tokens: {
    keyword: "#f92672",
    string: "#e6db74",
    comment: "#75715e",
    number: "#ae81ff",
    function: "#a6e22e",
    type: "#66d9ef",
    variable: "#f8f8f2",
    operator: "#f92672",
    constant: "#ae81ff",
    tag: "#f92672",
    attribute: "#a6e22e",
    property: "#66d9ef",
    punctuation: "#f8f8f2",
    background: "#272822"
  }
});
var nord = defineSyntaxTheme({
  name: "nord",
  tokens: {
    keyword: "#81a1c1",
    string: "#a3be8c",
    comment: "#616e88",
    number: "#b48ead",
    function: "#88c0d0",
    type: "#8fbcbb",
    variable: "#d8dee9",
    operator: "#81a1c1",
    constant: "#b48ead",
    tag: "#81a1c1",
    attribute: "#8fbcbb",
    property: "#88c0d0",
    punctuation: "#d8dee9",
    background: "#2e3440"
  }
});
var tokyoNight = defineSyntaxTheme({
  name: "tokyo-night",
  tokens: {
    keyword: "#9d7cd8",
    string: "#9ece6a",
    comment: "#565f89",
    number: "#ff9e64",
    function: "#7aa2f7",
    type: "#2ac3de",
    variable: "#c0caf5",
    operator: "#89ddff",
    constant: "#ff9e64",
    tag: "#f7768e",
    attribute: "#bb9af7",
    property: "#73daca",
    punctuation: "#9abdf5",
    background: "#1a1b26"
  }
});
var catppuccinMocha = defineSyntaxTheme({
  name: "catppuccin-mocha",
  tokens: {
    keyword: "#cba6f7",
    string: "#a6e3a1",
    comment: "#6c7086",
    number: "#fab387",
    function: "#89b4fa",
    type: "#f9e2af",
    variable: "#cdd6f4",
    operator: "#89dceb",
    constant: "#fab387",
    tag: "#f38ba8",
    attribute: "#f9e2af",
    property: "#94e2d5",
    punctuation: "#bac2de",
    background: "#1e1e2e"
  }
});
var githubLight = defineSyntaxTheme({
  name: "github-light",
  tokens: {
    keyword: "#cf222e",
    string: "#0a3069",
    comment: "#6e7781",
    number: "#0550ae",
    function: "#8250df",
    type: "#953800",
    variable: "#24292f",
    operator: "#cf222e",
    constant: "#0550ae",
    tag: "#116329",
    attribute: "#0550ae",
    property: "#0550ae",
    punctuation: "#24292f",
    background: "#ffffff"
  }
});
var githubDark = defineSyntaxTheme({
  name: "github-dark",
  tokens: {
    keyword: "#ff7b72",
    string: "#a5d6ff",
    comment: "#8b949e",
    number: "#79c0ff",
    function: "#d2a8ff",
    type: "#ffa657",
    variable: "#e6edf3",
    operator: "#ff7b72",
    constant: "#79c0ff",
    tag: "#7ee787",
    attribute: "#79c0ff",
    property: "#79c0ff",
    punctuation: "#c9d1d9",
    background: "#0d1117"
  }
});
var solarizedLight = defineSyntaxTheme({
  name: "solarized-light",
  tokens: {
    keyword: "#859900",
    string: "#2aa198",
    comment: "#93a1a1",
    number: "#d33682",
    function: "#268bd2",
    type: "#b58900",
    variable: "#657b83",
    operator: "#859900",
    constant: "#cb4b16",
    tag: "#268bd2",
    attribute: "#b58900",
    property: "#268bd2",
    punctuation: "#586e75",
    background: "#fdf6e3"
  }
});
var oneLight = defineSyntaxTheme({
  name: "one-light",
  tokens: {
    keyword: "#a626a4",
    string: "#50a14f",
    comment: "#a0a1a7",
    number: "#986801",
    function: "#4078f2",
    type: "#c18401",
    variable: "#e45649",
    operator: "#0184bc",
    constant: "#986801",
    tag: "#e45649",
    attribute: "#986801",
    property: "#e45649",
    punctuation: "#383a42",
    background: "#fafafa"
  }
});
var catppuccinLatte = defineSyntaxTheme({
  name: "catppuccin-latte",
  tokens: {
    keyword: "#8839ef",
    string: "#40a02b",
    comment: "#9ca0b0",
    number: "#fe640b",
    function: "#1e66f5",
    type: "#df8e1d",
    variable: "#4c4f69",
    operator: "#179299",
    constant: "#fe640b",
    tag: "#d20f39",
    attribute: "#df8e1d",
    property: "#179299",
    punctuation: "#6c6f85",
    background: "#eff1f5"
  }
});
var tokyoNightLight = defineSyntaxTheme({
  name: "tokyo-night-light",
  tokens: {
    keyword: "#8c4351",
    string: "#485e30",
    comment: "#9699a3",
    number: "#965027",
    function: "#34548a",
    type: "#166775",
    variable: "#343b58",
    operator: "#34548a",
    constant: "#965027",
    tag: "#8c4351",
    attribute: "#8f5e15",
    property: "#33635c",
    punctuation: "#343b58",
    background: "#d5d6db"
  }
});
var darkSyntaxPresets = [oneDarkPro, dracula, monokai, nord, tokyoNight, catppuccinMocha, githubDark];
var lightSyntaxPresets = [githubLight, solarizedLight, oneLight, catppuccinLatte, tokyoNightLight];
var allSyntaxPresets = [...darkSyntaxPresets, ...lightSyntaxPresets];
export {
  MediaTheme,
  SyntaxTheme,
  Theme,
  ThemeContext,
  borderDefaults,
  borderVars,
  colorDefaults,
  colorVars,
  dataTokenDefaults,
  defineSyntaxTheme,
  defineTheme,
  domainTokenDefaults,
  durationDefaults,
  durationVars,
  easeDefaults,
  easeVars,
  expandColorScale,
  expandMotionScale,
  expandRadiusScale,
  expandTypeScale,
  fontWeightDefaults,
  fontWeightVars,
  generateOnMediaCSS,
  generateThemeCSS,
  generateThemeCSSFlat,
  generateThemeRules,
  generateThemeRulesSplit,
  generateTypeScaleComponents,
  isDefinedTheme,
  radiusDefaults,
  radiusVars,
  resolveThemeToken,
  resolveThemeTokens,
  shadowDefaults,
  shadowVars,
  sizeDefaults,
  sizeVars,
  spacingDefaults,
  spacingVars,
  syntaxTokenDefaults,
  textSizeDefaults,
  textSizeVars,
  tokenDefaults,
  tokenVar,
  tokenVars,
  transitionDefaults,
  transitionVars,
  typeScaleDefaults,
  typeScaleVars,
  typographyDefaults,
  typographyVars,
  useSyntaxTheme,
  useTheme
};
//# sourceMappingURL=@astryxdesign_core_theme.js.map
